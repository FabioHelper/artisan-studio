# Dependencies — restore via `npm install` (package-lock.json is tracked)
node_modules/

# Deterministic-pipeline outputs (regenerated by the scripts)
.parity/
.parity-traces/

# Transient scratch produced by demos/tests
.tmp-broken/
*.log
